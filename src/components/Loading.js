import React from 'react';
import './loading.css';

class Loading extends React.Component {
  render() {
    return (
      <p className="loadingComponent">Carregando...</p>
    );
  }
}

export default Loading;
